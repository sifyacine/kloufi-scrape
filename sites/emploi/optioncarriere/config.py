# The base URL for your single page of job listings
BASE_URL = "https://www.optioncarriere.dz/emploi?s=&l=Alg%C3%A9rie&p=1"

# The CSS selector that targets each job card (same as before)
CSS_SELECTOR = "[class^='panel panel-clickable']"

# Define the required keys for a job to be considered complete
# (Adjust these to match what you consider "required")
REQUIRED_KEYS = ["titre"]


